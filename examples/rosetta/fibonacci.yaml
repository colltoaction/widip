- ? {}: 0
  ? nat
- ? !0 0: nat
  : ? nat
    ? nat
    ? nat
  ? nat
- ? !fib nat: nat
    ? nat
  : {}

  ? - !succ nat: nat
      ? nat
    - ? !fib nat: nat
        ? nat
      : {}
      ? nat
    - ? !plus
        ? nat
        !succ nat: nat
      : !fib nat
      ? - !succ nat
        - !succ nat
        - !fib nat
    - nat