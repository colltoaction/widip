? ? !plus
    {}: !0 nat
    !y nat: nat
  : nat
  ? !y nat: nat
: nat

? ? !plus
    !succ nat: !k nat
    !y nat: nat
  : - nat: nat

  ? ? !plus
      !k nat: nat
      !y nat: nat
    : nat: nat
  : !succ nat: nat
: nat
