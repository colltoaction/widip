# titi/repl.yaml
# !/usr/bin/env titi
# Read-Eval-Print Loop Implementation in YAML

!WhileLoop:
  test: !TRUE {}
  body:
    !Sequential:
      - !read: !stdin {}
      - !interpreter: { program: !yaml: !read: !stdin {}, arg: !stdin {} }
      - !write: !stdout {}
