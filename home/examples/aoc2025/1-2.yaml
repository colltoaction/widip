- !cat examples/aoc2025/1-1.input
- !tr {LR, -+}
- # TODO change simulation for math
  !awk |
    BEGIN { AT = 50 }
    {
      if ($1<0) {DIR = -1} else {DIR = 1}
      TICKS = DIR*$1
      while (TICKS-->0) {
        HITS += (AT+=DIR)%100 == 0
      }
    }
    END { print HITS }