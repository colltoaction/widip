#!/home/colltoaction/GitHub/widip/bin/titi
&countdown
!xargs { test, 0, -eq }: Liftoff!
!xargs { test, 0, -lt }:
  !seq
  - !print
  - !xargs { -I, "{}", sh, -c, "expr {} - 1 || true" }
  - *countdown
